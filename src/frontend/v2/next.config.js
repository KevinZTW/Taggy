/** @type {import('next').NextConfig} */
const nextConfig = {
    typescript: {
        //  TODO: Remove this when we have fixed all the errors
        ignoreBuildErrors: true,
    },
}

module.exports = nextConfig
