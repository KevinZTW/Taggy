import { app } from "./lib.js";

it("get Member tag folder ids", () => {
  const testUserUncaFolder = {
    id: "untestUser",
    name: "Uncategorized",
    tags: [],
  };
  expect("1").toEqual("1");
  return;
});
